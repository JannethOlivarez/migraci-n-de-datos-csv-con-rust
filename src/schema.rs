table! {
    personas (id) {
        id -> Int4,
        identificacion -> Varchar,
        nombre -> Varchar,
        genero -> Varchar,
        estado_civil -> Varchar,
        fecha_nacimiento -> Varchar,
        telefono -> Varchar,
        direccion -> Varchar,
        email -> Varchar,
        estado -> Int4,
        observacion -> Varchar,
    }
}
